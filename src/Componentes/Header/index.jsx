import './style.scss'

const Header = () => {
  return (
    <header className="header-portfolio">
      <nav>
        <a href="#">Início</a>
        <a href="#">Sobre Mim</a>
        <a href="#">Projetos</a>
        <a href="#">Habilidades</a>
        <a href="#">Certificações</a>
      </nav>
    </header>
  );
};

export default Header;
